Requires

   POSIX environment
   Standard POSIX /bin/sh
   curl(1) client

To install (as root)

   # Install without the *.sh extension.
   # System wide programs dont use extensions.

   install -m 755 ddns-updater.sh /usr/local/bin/ddns-updater
   install -m 644 cron.d/ddns-updater /etc/cron.d/

   mkdir -p ~/.ddns-updater

Configure for ducdns.org

   # Copy TOKEN from your account page
   # SUBDOMAIN.ddns-updater.org

   echo TOKEN > ~/.ddns-updater/duckdns.token
   echo SUBDOMAIN,SUBDOMAIN... > ~/.ddns-updater/duckdns.domains
